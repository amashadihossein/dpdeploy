# The dp_deploy is a wrapper around this. Reason: With S3 generic methods, function calls as defaults parameters are not recognized as the class of the object they return

The dp_deploy is a wrapper around this. Reason: With S3 generic methods,
function calls as defaults parameters are not recognized as the class of
the object they return

## Usage

``` r
dp_deployCore(conf, project_path, d, dlog, git_info, type, ...)
```
